import { ComingSoonPage } from "@/features/dashboard/components/coming-soon-page";

export default function GovernmentRulesPage() {
  return (
    <ComingSoonPage
      eyebrow="Scope Updated"
      title="Government Rules"
      description="Personal contribution control is now managed in My Settings. A shared admin rule editor is no longer part of the personal calculator scope."
    />
  );
}
