# Server Actions

Place server actions in this folder and keep them feature-scoped whenever possible.

Use zod schemas for input validation and call domain services instead of embedding heavy business logic in the action itself.
